---
# defaults file for cron
ansible_managed: "ANSIBLE MANAGED"


ansible_cron__head: |
  SHELL=/bin/bash
  PATH=/sbin:/bin:/usr/sbin:/usr/bin
  MAILTO=root


#ansible_cron__jobs:
#  - '0 7 * * * nobody /bin/true'
